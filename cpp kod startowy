#include <iostream>
using namespace std;

int main(){
    int tab[6] = {0};
    
    for(int i = 0; i <6; i++){
      for(int j = 0; j <5; j++){
        if(tab[j] > tab[j+1]){
          int temp = tab[j];
          tab[j] = tab[j+1];
          tab[j+1] = temp;
        }
      }
    }
}
